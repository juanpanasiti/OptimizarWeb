
<p>
  <strong>Nombre:</strong>
  <%= @category.name %>
</p>
<p>
	<strong>Articulos:</strong>
	<%= @category.articles.all.count %>
</p>
<p>
  <strong>Color:</strong>
  <%= @category.color %>
</p>

<%= link_to 'Editar', edit_category_path(@category) %> 
<%= link_to 'Volver', categories_path %>
